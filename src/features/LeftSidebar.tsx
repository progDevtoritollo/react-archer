import React from 'react';
import { Box, Typography } from '@mui/material';

const LeftSidebar: React.FC = () => {
      return (
            <Box sx={{ display: 'flex' }}>
                  <Typography variant='h6'>LeftSidebar</Typography>
            </Box>
      );
};
export default LeftSidebar;
